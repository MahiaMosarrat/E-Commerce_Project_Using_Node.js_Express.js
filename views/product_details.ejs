<!DOCTYPE html>
<html>
  <head>
    <title><%= product.product_title %></title>
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
    />
    <style>
      body {
        font-family: "Arial", sans-serif; /* Modern font */
        background-color: #f4f4f4; /* Light background */
        color: #333; /* Dark text for contrast */
      }

      .container {
        margin-top: 50px;
        padding: 30px;
        background-color: #fff;
        border-radius: 10px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        width: 60%;
      }

      .product-title {
        font-size: 2.8rem;
        font-weight: 700;
        margin-bottom: 20px;
        text-align: center;
        color: #2c3e50; /* Darker heading color */
      }

      .product-image {
        text-align: center;
        margin-bottom: 30px;
      }

      .product-image img {
        max-width: 60%;
        height: auto;
        border-radius: 10px;
        box-shadow: 0 8px 15px rgba(0, 0, 0, 0.15);
        transition: transform 0.3s ease; /* Smooth zoom on hover */
        padding: 25px 0;
      }

      .product-image img:hover {
        transform: scale(1.05); /* Slightly zoom on hover */
      }

      .product-description {
        font-size: 1.2rem;
        line-height: 1.7;
        margin-bottom: 25px;
      }

      .product-price {
        font-size: 1.5rem;
        font-family: Verdana, Geneva, Tahoma, sans-serif;
        font-weight: 600;
        color: #bd1c0a; /* Red price */
        margin-bottom: 30px;
        text-align: center;
      }

      .product-actions {
        text-align: center;
      }

      .btn-primary {
        background-color: hsl(174, 81%, 23%); /* Blue button */
        border-color: #29e39f;
        padding: 12px 25px;
        font-size: 1.1rem;
        font-family: "Trebuchet MS", "Lucida Sans Unicode", "Lucida Grande",
          "Lucida Sans", Arial, sans-serif;
        font-weight: 500;
        border-radius: 8px;
        transition: background-color 0.3s ease;
      }

      .btn-primary:hover {
        background-color: #0eb986;
        border-color: #056e5b;
      }

      .back-link {
        display: inline-block;
        margin-top: 20px;
        color: #fefefe;
        transition: color 0.3s ease;
      }
      .back-link:hover {
        color: #fdfdfd;
      }

      /* Responsive Design */
      @media (max-width: 768px) {
        .product-image img {
          max-width: 90%;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1 class="product-title"><%= product.product_title %></h1>
      <div class="product-image">
        <img
          src="<%= product.image_path %>"
          alt="<%= product.product_title %>"
          class="img-fluid"
        />
      </div>
      <div class="product-description" style="text-align: center">
        <%= product.product_description %>
      </div>
      <div class="product-price">Price: <%= product.product_price %> TK</div>
      <div
        class="product-actions"
        style="
          display: flex;
          justify-content: center;
          gap: 10px;
          align-items: center;
        "
      >
        <div>
          <a href="/shop" class="btn btn-primary back-link"
            ><i class="fas fa-arrow-left"></i> Back Shop</a
          >
        </div>

        <div style="margin-top: 17px">
          <form action="/cart/add" method="POST" class="text-center">
            <input type="hidden" name="productId" value="<%= product.id %>" />
            <button
              type="submit"
              class="btn btn-primary add-to-cart-button"
              style="color: #fff"
            >
              Add to Cart <i class="fas fa-arrow-right"></i>
            </button>
          </form>
        </div>

        <!-- <a href="/cart" class="btn btn-primary back-link"
          >Add Cart <i class="fas fa-arrow-right"></i>
        </a> -->
      </div>
    </div>
  </body>
</html>
